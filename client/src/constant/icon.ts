import {
  faClock,
  faQuestionCircle,
  faSearch,
  faTimes,
} from '@fortawesome/free-solid-svg-icons'

const myIcon = {
  clock: faClock,
  question: faQuestionCircle,
  search: faSearch,
  close: faTimes,
}

export default myIcon
