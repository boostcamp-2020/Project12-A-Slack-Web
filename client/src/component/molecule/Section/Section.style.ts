import styled from 'styled-components'

const SectionContainer = styled.div`
  width: 100%;
`

const SectionHeader = styled.div`
  width: 100%;
  padding: 5px;
`

export default {
  SectionContainer,
  SectionHeader,
}
